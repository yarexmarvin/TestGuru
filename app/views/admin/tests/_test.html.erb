<tr>
  <td><%= test.title %></td>
  <td><%= level_translation(test.level) %></td>
  <td><%= questions_amount(test) %></td>
  <td><%= link_to 'show', admin_test_path(test)%></td>
  <td><%= link_to 'edit', edit_admin_test_path(test) %></td>
  <td>
    <%= link_to 'delete', admin_test_path(test), method: :delete, data: { confirm:
    'Are you sure?' } %>
  </td>
</tr>
