<tr>
  <td><%= link_to t('system.button.show'), admin_question_path(question) %></td>
  <td>
    <%= link_to t('system.button.edit'), edit_admin_question_path(question) %>
  </td>
  <td>
    <%= link_to t('system.button.delete'), admin_question_path(question), method: :delete, data: {confirm: t('system.modal.confirm') } %>
  </td>
  <td><%= question.body %></td>
</tr>
