<h3><%= @question.test.title %> question</h3>
<p><%= @question.body %></p>
<hr>
  <%= render @question.answers%>
<hr>
<%= link_to 'Back', admin_test_path(@question.test) %> | <%= link_to 'Edit',
edit_admin_question_path(@question) %> | <%= link_to 'Delete',
admin_question_path(@question), method: :delete, data: {confirm: "Are you sure?"} %>
<%= link_to 'New answer', new_admin_question_answer_path(@question) %>
