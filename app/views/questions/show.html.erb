<h3><%= @question.test.title %> question</h3>
<p><%= @question.body %></p>
<hr />
<%= link_to 'Back', test_path(@question.test_id) %> | <%= link_to 'Edit',
edit_question_path(@question) %> | <%= link_to 'Delete',
question_path(@question), method: :delete, data: {confirm: "Are you sure?"} %>
